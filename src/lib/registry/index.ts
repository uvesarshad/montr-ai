/**
 * Capability Registry — public barrel (the M3 Capability-Hub seam).
 *
 * Import from `@/lib/registry` for the descriptor types, the singleton, and the
 * core-capability sync adapters.
 */
export * from './capability-registry';
export * from './sync';
