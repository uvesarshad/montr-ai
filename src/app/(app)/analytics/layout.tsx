/**
 * Analytics module layout. The module SubNav is shell chrome — rendered on
 * the gutter by `(app)/layout.tsx` from `@/components/shell/subnav-registry`
 * (ANALYTICS_RAIL).
 */
export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
