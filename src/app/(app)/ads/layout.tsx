/**
 * Ads module layout. The module SubNav is shell chrome — rendered on the
 * gutter by `(app)/layout.tsx` from `@/components/shell/subnav-registry`
 * (ADS_RAIL).
 */
export default function AdsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
